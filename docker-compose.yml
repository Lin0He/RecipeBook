services:
  recipebook-api:
    build: .
    container_name: recipebook-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
